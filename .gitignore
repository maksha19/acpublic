# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment. GitHub Pages free tier requires a PUBLIC repository, so anything
# committed here is world-readable. *.local already covers .env.local, but be
# explicit — a stray `.env` would otherwise be published.
.env
.env.*
!.env.example
